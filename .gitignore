# 这是Git的配置文件,里面列出来的是要排除掉的,不需要提交到Git库中的文件
.DS_Store
node_modules
/dist

# local env files (全局变量的文件)
.env.local
.env.*.local

# Log files (debug文件)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files (设置不需要提交到git的文件,这里的是编辑器配置文件)
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*
